<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareConnect">
        <activity
            android:name=".ui.activity.LabTest.LabOrderActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.payment.ReceiptActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.payment.PaymentActivity"
            android:exported="false" /> <!-- 🔥 AUTH -->
        <activity android:name=".ui.activity.Auth.LoginActivity" />
        <activity android:name=".ui.activity.home.HomeActivity" />
        <activity android:name=".ui.activity.Auth.RegisterActivity" />
        <activity android:name=".ui.activity.Auth.ForgotActivity" />
        <activity android:name=".ui.activity.Auth.ResetActivity" /> <!-- 🔥 HOME -->
        <activity
            android:name=".ui.activity.Auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🔥 DOCTOR -->
        <activity android:name=".ui.activity.Doctor.FindDoctorActivity" />
        <activity android:name=".ui.activity.Doctor.DoctorListActivity" />
        <activity android:name=".ui.activity.Doctor.DoctorDetailsActivity" /> <!-- 🔥 APPOINTMENT -->
        <activity android:name=".ui.activity.Appointment.BookAppointmentActivity" />
        <activity android:name=".ui.activity.Appointment.MyAppointmentsActivity" /> <!-- 🔥 MEDICINE -->
        <activity android:name=".ui.activity.Medicine.BuyMedicineActivity" />
        <activity android:name=".ui.activity.Medicine.BuyMedicineDetailsActivity" />
        <activity android:name=".ui.activity.Medicine.BuyMedicineBookActivity" />
        <activity android:name=".ui.activity.Medicine.CartBuyMedicineActivity" /> <!-- 🔥 LAB TEST -->
        <activity android:name=".ui.activity.LabTest.LabTestActivity" />
        <activity android:name=".ui.activity.LabTest.LabOrderHistoryActivity" />
        <activity android:name=".ui.activity.LabTest.LabTestBookActivity" />
        <activity android:name=".ui.activity.LabTest.CartLabActivity" /> <!-- 🔥 ARTICLES -->
        <activity android:name=".ui.activity.Articles.HealthArticleActivity" />
        <activity android:name=".ui.activity.Articles.HealthArticleDetailsActivity" /> <!-- 🔥 ORDERS -->
        <activity android:name=".ui.activity.Orders.OrderDetailsActivity" /> <!-- 🔥 MAIN -->
        <activity android:name=".MainActivity" />
    </application>

</manifest>